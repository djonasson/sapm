.navbar.navbar-fixed-top
  .navbar-inner
    .container-fluid
      %a{ class: "btn btn-navbar", data: { toggle: "collapse", target: ".nav-collapse" } }
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar

      %a.brand{ href: root_url  } SAPM

      - if true #TODO: user_signed_in?
        .btn-group.pull-right
          %a{ class: "btn dropdown-toggle", data: { toggle: "dropdown" }, href: "#" }
            %i.icon-user
            = "Daniel" #TODO: current_user.display_name
            %span.caret
          %ul.dropdown-menu
            %li= link_to('Menu 1', '#')
            %li.divider
            %li= link_to(t('.my_account', default: 'My Account'), '#') #TODO:edit_user_registration_path)
            %li.divider
            %li= link_to(t('.sign_out', default: 'Sign-out'), '#') #TODO:destroy_user_session_path, method: 'delete')
      - else
        .pull-right
          %ul.nav
            %li= link_to(t('.sign_in', default: 'Sign-in'), '#') #TODO:new_user_session_path)
            %li= link_to(t('.sign_up', default: 'Sign-up'), '#') #TODO:new_user_registration_path)

      .nav-collapse
        = tabs

      %form.navbar-search.pull-left
        %input{ type: "text", class: "search-query", placeholder: "Search"}
