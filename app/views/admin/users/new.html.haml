.page-header
  = title('New User')

= simple_form_for(@user, url: admin_users_path, html: { method: :post, class: 'form form-horizontal' }) do |f|

  = f.error_notification

  .form-inputs
    = f.input :name, as: :string, wrapper: :append do
      = f.input_field :name, autofocus: true
      %span.add-on>
        %i.icon-user

    = f.input :email, required: true, as: :email, wrapper: :append do
      = f.input_field :email
      %span.add-on>
        %i.icon-envelope

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
    = link_to "Cancel", admin_users_path, class: 'btn'
